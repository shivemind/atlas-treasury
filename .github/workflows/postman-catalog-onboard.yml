name: Postman Catalog Onboard

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

permissions:
  contents: read

jobs:
  collection-run:
    name: Run API Collection
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Postman CLI
        run: curl -o- "https://dl-cli.pstmn.io/install/linux64.sh" | sh

      - name: Login to Postman
        run: postman login --with-api-key ${{ secrets.POSTMAN_API_KEY }}

      - name: Run Collection
        run: |
          postman collection run 52400725-d016eb95-2657-4faa-88b6-3188a46185ff \
            -e 52400725-742997bb-d2c1-47e8-b543-dcc97e4b9480 \
            --postman-api-key ${{ secrets.POSTMAN_API_KEY }}

      - name: Trigger Monitor Run
        run: |
          curl -sf -X POST "https://api.getpostman.com/monitors/52400725-1f11e404-a63c-49f0-a996-a21844b68990/run" \
            -H "x-api-key: ${{ secrets.POSTMAN_API_KEY }}"

      - name: Summary
        if: always()
        run: |
          echo "### Postman Catalog Onboard - Atlas Treasury" >> $GITHUB_STEP_SUMMARY
          echo "- **Collection**: 52400725-d016eb95-2657-4faa-88b6-3188a46185ff" >> $GITHUB_STEP_SUMMARY
          echo "- **Monitor**: 52400725-1f11e404-a63c-49f0-a996-a21844b68990" >> $GITHUB_STEP_SUMMARY
